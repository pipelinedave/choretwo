name: Deploy to Staging (Manual)

on:
  workflow_dispatch:
    inputs:
      version_tag:
        description: 'Image tag to deploy (default: latest)'
        required: false
        default: 'latest'

jobs:
  deploy:
    name: Trigger Staging Deployment
    uses: ./.github/workflows/update-kubernetes-deployment.yaml
    with:
      overlay: staging
      version_tag: ${{ inputs.version_tag || 'latest' }}
    secrets: inherit
