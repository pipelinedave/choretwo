# Monolith requirements — consolidated from chore-service, log-service, notification-service
# ai-copilot-service is excluded (runs as separate container)
#
# Serverless-Diet (Vercel Python Runtime): asyncpg, alembic, redis und celery
# sind im Runtime-Pfad ungenutzt (redis/celery nur in vendor/notify/app/
# celery_app.py + tasks.py, die von keiner Route importiert werden) und wurden
# entfernt. Test-Dependencies (pytest*) zieht CI aus services/<svc>/
# requirements.txt, nicht aus dieser Datei — für lokale Monolith-Tests:
#   pip install pytest pytest-asyncio
fastapi==0.109.0
uvicorn[standard]==0.27.0
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
python-dotenv==1.0.0
pydantic==2.5.3
httpx==0.26.0
PyJWT==2.8.0
python-multipart==0.0.9
