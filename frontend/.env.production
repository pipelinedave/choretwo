# Production API configuration
# Both monolith and auth service are behind the same nginx ingress.
# VITE_API_BASE and VITE_AUTH_BASE point to the same domain.

VITE_API_BASE=https://choretwo.stillon.top
VITE_AUTH_BASE=https://choretwo.stillon.top
